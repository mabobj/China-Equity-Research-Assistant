"""Reserved dataset name for future review report daily snapshots.

This round keeps review reports computed in-memory within one request, while the
workspace bundle exposes freshness metadata using the same day-level policy.
"""
