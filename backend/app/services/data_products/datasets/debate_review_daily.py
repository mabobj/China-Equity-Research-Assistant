"""Reserved dataset name for future debate review daily snapshots.

This round keeps debate reviews computed in-memory within one request, while the
workspace bundle exposes freshness metadata using the same day-level policy.
"""
