"""Reserved dataset name for future strategy plan daily snapshots.

This round keeps strategy plans computed in-memory within one request, while the
workspace bundle exposes freshness metadata using the same day-level policy.
"""
